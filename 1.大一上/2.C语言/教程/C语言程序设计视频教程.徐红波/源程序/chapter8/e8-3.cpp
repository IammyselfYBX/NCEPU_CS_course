#include "stdafx.h"

char max(float x, float y);

void main()
{
	float a, b;
	float c;
	scanf("%f %f", &a, &b);

	//c=max(a, b);
	c=max(a, b);

	printf("Max is %d\n", c);	
}

char max(float x, float y)
{
	//float z;
	//z =  x > y ? x: y;
	//return z;
	return 3.5;
}
